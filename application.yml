spring:
  application:
    name: CONFIG-DEFAULT

eureka:
  client:
    allow-redirects: true
    fetch-registry: true
    service-url:
      defaultZone : http://localhost:8761/eureka

  instance:
    prefer-ip-address: true
